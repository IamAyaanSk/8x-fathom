# F2 — Progress

- [x] Feature docs (`IMPLEMENTATION.md`, `PROGRESS.md`, `DECISIONS.md`)
- [x] Better Auth instance + Prisma adapter + Google provider
- [x] Express 5 `toNodeHandler` before `json()`
- [x] CORS `WEB_ORIGIN` + credentials
- [x] `requireSession` on `/api/v1`; users scoped to session
- [x] Vite `/api` proxy, auth client, axios `withCredentials`
- [x] `/login` AuthShell + Google CTA
- [x] `_authenticated` beforeLoad for `/` and `/users`
- [x] `AGENTS.md` F2 done + README auth env
- [x] lint / check-types
